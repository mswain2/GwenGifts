# SMTP configuration for email sending
# Copy this file to email/.env and replace values with your SMTP provider settings.

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your-smtp-password
SMTP_FROM_NAME=Gwyneth's Gift
